AIpacken AI Platform

This project ships without external cloud SDKs. All runtime dependencies are
declared in the per-component pyproject.toml / package.json manifests and are
pulled at build time from the public PyPI and npm registries.
