# ---------- Python ----------
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.joblib
*.pkl

# ---------- Node / web ----------
node_modules/
dist/
build/
.vite/
.turbo/
*.tsbuildinfo
# pnpm workspace — npm lockfiles should never be committed
apps/web/package-lock.json
# pnpm store + lockfile — lockfile is deliberately absent pending a
# tracked dep-management pass (docs/review/CODE_REVIEW_REPORT.md SOTA gap).
apps/web/.pnpm-store/
apps/web/pnpm-lock.yaml
# TS-compiled config artifacts (real sources are .ts siblings)
apps/web/vite.config.js
apps/web/vite.config.d.ts
apps/web/tailwind.config.js
apps/web/tailwind.config.d.ts

# ---------- IDEs ----------
.vscode/
.idea/
.DS_Store

# ---------- Env / secrets ----------
.env
.env.local
.env.*.local
!.env.example

# ---------- Data / artifacts (local) ----------
*.csv
*.xlsx
*.xls
*.parquet
*.pdf
.ipynb_checkpoints/

# ---------- Docker / infra ----------
*.log
.postgres_data/
.platform_data/

# ---------- Design ----------
# DESIGN.zip is checked in as the source-of-truth for the AIpacken brand
!DESIGN.zip

# ---------- Generated ----------
packages/api-client/src/schema.d.ts

# ---------- Claude Code ----------
.claude/settings.local.json
cookie.jar

# ---------- Review reports (local only — security findings with file:line anchors) ----------
docs/review/
